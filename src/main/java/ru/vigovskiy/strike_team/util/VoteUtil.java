package ru.vigovskiy.strike_team.util;

public class VoteUtil {

//    public static Vote createVoteFromDto(VoteDto dto) {
//        return new Vote(dto.getDecisionType(), dto.getUserId(), dto.getVoteDayId());
//    }
//
//    public static VoteDto createDtoFromVote(Vote vote) {
//        return new VoteDto(vote.getUserId(), vote.getVoteDayId(), vote.getDecisionType());
//    }
//
//    public static List<VoteDto> createDtosFromVotes(List<Vote> votes) {
//        return Optional.ofNullable(votes).orElse(Collections.emptyList()).stream()
//                .map(VoteUtil::createDtoFromVote).collect(Collectors.toList());
//    }
}
